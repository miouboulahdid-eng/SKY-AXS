#!/bin/sh
# Safe simulated SQLi PoC (simulation only)
TARGET="${AXS_TARGET:-$1}"
EXTRA="${AXS_EXTRA:-$2}"
echo "PoC: SQL Injection (SIMULATED)"
echo "Target: $TARGET"
echo "Extra: $EXTRA"
# simulate checking parameter
echo "Simulating boolean-based SQLi check..."
sleep 1
echo "Result: VULNERABLE_SIMULATED"  # simulation marker
exit 0
